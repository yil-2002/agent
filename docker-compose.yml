version: '3.8'

services:
  omni-router:
    image: diegosouzapw/omniroute:latest
    container_name: omni-router
    restart: unless-stopped
    ports:
      - "8000:8000"
    deploy:
      resources:
        limits:
          memory: 512M

networks:
  default:
    name: app-network
