services:
  omni-router:
    image: diegosouzapw/omniroute:latest
    container_name: omni-router
    restart: unless-stopped
    ports:
      - "8080:8000"
    deploy:
      resources:
        limits:
          memory: 512M

networks:
  default:
    name: app-network
